# 网站自动化需求优先级 → 仓库代码对照表

对照源：`~/Downloads/网站页面全自动化流程-2026-2.xlsx` 的「需求优先级」表（17 条）。
本表把每条需求逐条对到本仓库的实际代码、定时任务、依赖和当前状态。

## 分工

- **工作流负责人**：只管 MCP 编排器内核和 skill 底层——`mcp-agentos/agentos_mcp_orchestrator_transfer/core/`、`mcp_server.py`、各 adapter 的 `tools.py` 注册层、`config.example.yaml`。这部分是"引擎"，不直接对应 17 条业务需求里的任何一条。
- **网站与定时任务负责人**：17 条业务需求全部归他，包括 X 平台（需求 17）。代码散在 `mcp-agentos/.../adapters/wp_tes/runtime/`、`mcp-agentos/.../adapters/{dashboard_metrics,competitor_seo,iweaver_admin}/`、`edm/edm-system/`、`analytics/`、`x-automation/`。

引擎（工作流负责人）跑业务（网站负责人）写的适配器，两层分开维护。

---

## 17 条逐条对照

### 需求 1 · 优化 prompt 让生成结果更好（30 分→60 分）
- **优先级/状态**：— / 完成
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/prompts.py` — 各类生成任务的 prompt 模板
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/assets/prompts.json` — 可调参数化的 prompt 配置
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/core/prompt_registry.py` — prompt 注册与加载机制（工作流负责人维护）
- **说明**：内容生成质量由 prompt 决定。改 prompt 就改上面两个文件。验收标准是文案审核从 30 分提到 60 分。
- **依赖**：无外部依赖，纯文本配置。

### 需求 2 · 分类页/聚合页图片与描述
- **优先级/状态**：— / 完成（4/20）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/blog_generat.py` — `process_cover_image()` 处理封面图
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/assets/page_layouts/` — 页面布局模板（`all.html` / `upload-input.html` / `upload-link.html` / `upload.html`）
- **规格**：分类固定 5 个（AI Analysis、AI Mind Map、AI Summary、AI Writing、AI Converter）；聚合页图片固定 7 种（见 WP 媒体库名称：`Al Poetry Composer`、`Brand Reputation Analysis`、`Video Sentiment Analyzer`、`logo idea generator`、`Historical Event Thread Mapper`、`Al-Handwriting-Recognition`、`Instagram-Name-Generator`）；聚合页描述 75–115 字符（含空格）。
- **验收**：准确填充，填充内容 OK。

### 需求 3 · 自动生成 TDK 并填进 Rank Math
- **优先级/状态**：— / 完成（4/21）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/blog_generat.py` — TDK 生成逻辑
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/assets/wordpress_plugins/custom-rank-math-rest-fields.php` — WP 插件，通过 REST API 自动填 Rank Math 字段
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/assets/wp_mu_plugins/rest-api-rankmath-meta.php` — MU 插件辅助
  - 打包版：`custom-rank-math-rest-fields.zip`、`iweaver-rank-math-rest-bridge.zip`
- **规格**：Title 50–60 字符含关键词、参考竞品决定是否带 online/free、结尾 `| iWeaver`；Description 120–155 字符植入 1–2 个长尾词；Keyword 5 个。
- **依赖**：WP 装好 Rank Math 插件 + 上述自研桥接插件。

### 需求 4 · use case 图片生成自动化
- **优先级/状态**：— / 完成（4/22）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/blog_generat.py` — `generate_blog(keyword, generate_cover=True)` 内调 Gemini 出 4 张风格一致的 PNG
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/insight_article.py` — `generate_article_images(image_prompts, slug)` 另一套图片生成路径
- **规格**：每个 use case 单独 prompt，Gemini 生成 4 张图；转 webp，命名小写英文+连字符；WP 媒体库上传后插入对应位置。
- **验收**：图符合网站调性、不带其他平台 logo。
- **依赖**：Gemini API key（在 124 的 orchestrator `.env`，`COVER_IMAGE_API_KEY` 等；交接时见飞书敏感清单）。

### 需求 5 · 填充到飞书云文档-Excel 表
- **优先级/状态**：P1 / 完成
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/feishu_docx.py` — 飞书文档读写
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/feishu_bot.py` — 飞书机器人推送（`send_card()`、`reply_text()`）
- **说明**：每次发布后把 URL、日期、名称、ID 等写进飞书云文档，避免人工维护发布登记表。
- **依赖**：飞书自建应用凭据（`FEISHU_APP_ID`/`FEISHU_APP_SECRET`，在 124 `.env`，敏感清单）。

### 需求 6 · Blog 文章自动化
- **优先级/状态**：P0 / 完成（4/27）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/blog_generat.py` — `generate_blog(keyword, generate_cover=True)` 主流程：谷歌搜前五 → 爬正文提 H2/H3 → YouTube/Twitter/Reddit 补充 → AI 生成图文 → 给 TDK 封面 URL → 发 WP → 提交 GSC 收录
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/html_generat.py` — `generate_seo_content(keyword)` HTML 正文生成
  - 触发：`feishu_bot.py` 的 `execute_publish_blog(keyword, chat_id, message_id)`——飞书机器人指令触发
- **定时**：按需触发（飞书机器人指令），非定时。
- **依赖**：SERPAPI（`SERPAPI_KEY`）、Firecrawl（`FIRECRAWL_API_KEY`）、Coze/LLM（`coze_llm.py`）、WP REST API、GSC。

### 需求 7 · 热点内容自动化
- **优先级/状态**：P0 / 完成（4/27）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/blog_generat.py` — `generate_hot_topic_blog(niche)` 热点博客生成
  - 触发：`feishu_bot.py` 的 `execute_hot_topic(niche, chat_id, message_id)`
- **流程**：从公众号/Twitter/Reddit/大厂官方抓 AI 大模型热点 → 谷歌热点 blog 查询 top5 → 改写含新闻+社交讨论方向 → 给 TDK 封面 URL（去掉年份）→ 发 WP 提交收录。
- **依赖**：同需求 6。

### 需求 8 · 优化自动化（数据跌了就改）
- **优先级/状态**：P0 / 完成（4/29）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/content_audit.py` — 核心文件
    - `fetch_published_posts()` 拉已发布页
    - `count_words()` / `extract_faq_count()` / `extract_use_case_count()` 内容质量统计
    - 对比 GA 7天 vs 前7天、GSC 曝光点击变化，判断涨跌
- **判断逻辑**（见 xlsx「优化自动化流程」表）：跌了对比前五/前十竞品补缺、改 TDK、记录优化时间，持续监测 1–2 周。
- **依赖**：GA、GSC、Superset（数据源）。

### 需求 9 · 后台 Agent 上线自动化
- **优先级/状态**：P1 / **开发中**
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/iweaver_admin/tools.py` — 已有骨架，调 iWeaver Admin API 创建/管理后台 Agent
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/iweaver_admin/__init__.py`
- **依赖**：`IWEAVER_ADMIN_TOKEN`（JWE 加密，在 124 `.env`，敏感清单；**已暴露过，建议轮换**）、`IWEAVER_ADMIN_API_BASE`（test/prod）。
- **接手要做**：补完骨架，跑通 Agent 上下线流程。参考 `~/Downloads/后台Agents新增自动化工作流需求-202606.md`。

### 需求 10 · 裂变
- **优先级/状态**：P2 / **待做**
- **代码位置**：无
- **说明**：需求文档没写细节。接手后找产品确认要不要做、做什么。

### 需求 11 · 点踩点赞回复自动化
- **优先级/状态**：P0 / 完成（5/8）
- **代码位置**：
  - 仓库内脱敏参考版：`mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/wp_tes/runtime/feedback_auto_reply.py`
    - `fetch_new_records(processed)` 拉新反馈
    - `analyze_feedback(input_data)` 分析
    - `send_gmail()` / `send_feishu_card()` 回复与通知
  - 生产运行版：`edm/edm-system/feedback_service.py`（124 `/opt/edm-system/`，systemd `feedback-autoreply.service`，每分钟跑）
  - lib：`edm/edm-system/lib/feedback_analyzer.py`、`edm/edm-system/lib/feishu_notifier.py`、`edm/edm-system/lib/superset_client.py`
- **当前状态**：✅ 正常运行，每分钟 `Processed 0 new feedbacks`，Superset 登录正常。
- **依赖**：Superset（反馈数据源）、飞书（通知）、Gmail（邮件回复）。

### 需求 12 · EDM 邮件自动回复
- **优先级/状态**：P0 / 完成（5/13）
- **代码位置**：
  - `edm/edm-system/edm_service.py` — 主服务（124 `/opt/edm-system/`，systemd `edm-auto-reply.service`）
  - `edm/edm-system/feedback_service.py` — 反馈分支
  - lib：`edm/edm-system/lib/`（`config.py`、`email_utils.py`、`faq_matcher.py`、`filter_rules.py`、`queue.py`、`llm_client.py`、`gmail_client.py`、`feishu_notifier.py`、`template_engine.py`、`superset_client.py`）
  - FAQ 知识库：`edm/edm-system/faq_knowledge.json`
- **当前状态**：❌ **挂了**。Gmail 服务账号私钥被 Google 撤销（`invalid_grant: Invalid JWT Signature`），读不了 Gmail，连续失败 6 次触发飞书预警。**接手第一件事：去 Google Cloud Console 重新生成 `service-account.json`，替换 `/opt/edm-system/service-account.json`，重启 `edm-auto-reply.service`。**详见 `HANDOVER.md` 待办。
- **依赖**：Gmail service account、LLM（走 CLIProxy 8317 或 New API 8318）、飞书、Superset、代理（mihomo 10809 出口，访问 Google）。
- **相关排障历史**：HTML 解析、多语言、退款意图、通知过滤等修复记录见 git 提交历史。

### 需求 13 · 会员日志
- **优先级/状态**：P0 / 完成
- **代码位置**：
  - `analytics/iweaver-monitoring/monitoring/jobs/daily_monitor_report.py` — 每日会员日志报告
  - `analytics/iweaver-monitoring/monitoring/event_store.py` — 事件存储
  - `analytics/iweaver-monitoring/monitoring/feishu_sender.py` — 推飞书
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/dashboard_metrics/runtime/` — 指标采集（`google_metrics.py`、`sql_metrics.py`、`schema_probe.py`、`storage.py`）
- **定时**：`infra/systemd/iweaver-monitor-daily-report.timer`
- **依赖**：Superset、GSC/GA4、飞书。

### 需求 14 · 效果评测
- **优先级/状态**：P0 / 完成
- **代码位置**：
  - `analytics/iweaver-monitoring/monitoring/jobs/daily_monitor_report.py` — 同上，每日跑
  - `analytics/iweaver-monitoring/monitoring/jobs/dashboard_window_alert.py` — 窗口告警
  - `analytics/iweaver-monitoring/monitoring/card_templates.py` — 评测卡片渲染
- **定时**：`iweaver-monitor-daily-report.timer`（日报）、`iweaver-monitor-window-alert.timer`（窗口告警）
- **依赖**：Superset、飞书。

### 需求 15 · 自动化看板
- **优先级/状态**：P1 / 完成（5/31）
- **代码位置**：
  - `analytics/multisite-dashboard/` — 多站点周报面板（iWeaver/Palmly/Learning Coach）
    - `app.py` 主服务、`collector.py` 采集、`metrics.py` 指标、`storage.py` 存储、`superset_client.py`、`auth.py`、`fix_types.py`
    - 前端：`static/app.css`、`static/app.js`、`templates/index.html`、`templates/login.html`
    - 测试：`tests/test_dashboard.py`
    - schema：`schema.sql`
  - Superset 看板本身（不在仓库，在 8 服务器 Superset；指标口径与 SQL 见 `analytics/` 相关说明与 `HANDOVER.md`）
- **定时**：`infra/systemd/multisite-weekly-collector.timer`、`dashboard-metrics-daily.timer`、`dashboard-metrics-alert.timer`、`infra/systemd/dashboard-web.service`
- **访问**：面板端口 8780；Superset `galaxy.iweaver.ai`
- **依赖**：Superset 账号（`SUPERSET_USER/PASS`，敏感清单）、面板登录账号（`/etc/multisite-weekly-dashboard-auth.json`，hash+salt）。
- **当前状态**：✅ 正常。今早 `dashboard-metrics-daily` 报过一次 GSC/GA4 SSL EOF（代理访问 Google 偶发），盯着看会不会自愈。

### 需求 16 · 竞品调研（基于网站）
- **优先级/状态**：P2 / 完成（6/5）
- **代码位置**：
  - `mcp-agentos/agentos_mcp_orchestrator_transfer/adapters/competitor_seo/`
    - `tools.py` — 适配器入口
    - `runtime/` — `crawl_fetcher.py`（爬取）、`seo_extractor.py`（SEO 提取）、`technical_checker.py`（技术检查）、`report_generator.py`（报告）、`llm_client.py`
    - `assets/prompts.json` — 调研 prompt
- **说明**：基于网站做竞品 SEO 调研，自动出报告。详见 `~/Downloads/竞品 SEO 调研自动化工具.md`。

### 需求 17 · X 平台运营自动化
- **优先级/状态**：— / 开发中
- **代码位置**：`x-automation/` 整个目录
  - **控制台**：`x-automation/console/`
    - `app.py`（1109 行）— Flask 风格 stdlib HTTP 服务，端口 8790；含账号管理、排程、预算、运行记录、内容工作流（AI 评论）、选题发布（AI 发帖）、写入操作 BFF
    - `feishu_records.py` — 评论/发帖记录写飞书表（`record_reply`/`record_post`）
    - `workflow_llm.py` — 工作流 LLM（选题归纳、正文/评论生成）
    - `x_https_gateway.py` — 8443→8790 TLS 反代（X OAuth 回调用）
    - `static/app.css`、`static/app.js`、`templates/index.html` 等 — 控制台前端
    - `x-browse-console.env.example`、`x-browse-console.service.template`
  - **写入服务**：`x-automation/write-service/`
    - `x_write/`（`executor.py` 状态机、`xclient.py` X API、`oauth.py`、`credentials.py`、`repository.py`、`validation.py`、`http_service.py`、`config.py`、`db.py`、`auth.py`、`__main__.py`）
    - 端口 8791（loopback），草稿→冻结审批→执行→审计；当前 `global_write_paused=true`
    - `secrets.example.json`、`x_write.example.json`、`systemd/x-write-staging.service`
    - 测试：`tests/`（`test_console_bff.py`、`test_end_to_end_x.py`、`test_executor.py`、`test_foundation.py`、`test_media_upload.py`、`test_oauth_onboarding.py`、`test_xclient.py`）
  - **Windows worker**：`x-automation/worker/`
    - `worker.py`（1343 行）— AdsPower + geckodriver 只读浏览，3 并发槽
    - `install-worker.ps1` / `start-worker.ps1` / `uninstall-worker.ps1` — Windows 安装脚本
    - `worker.example.json` / `worker.example.production.json`
    - `X-Browse-Console-Worker.scheduled-task.xml`
    - `REDACTION-NOTES.md` — 脱敏说明
  - **部署脚本**：`x-automation/deploy/deploy_x_postflow.py`、`deploy_x_workflow.py`（暂停态部署：备份→停服→scp→起服→验证不变量）
  - **测试**：`x-automation/tests/`（`test_ui_static_contract.py` UI 静态合约、`test_workflow.py`、`test_oauth_bff.py`、`test_controller_lifecycle.py`、`test_worker_supervisor.py`、`test_integration_verification.py`、`test_feishu_records.py`）
- **运行方式**：控制台在 124（systemd `x-browse-console` + `x-https-gateway`）；worker 在 Windows 主机（10.0.2.123，AdsPower 10 个 Profile，计划任务）；写入服务在 124（systemd `x-write-service`，暂停态）。
- **当前模式**：手动模式——AI 生成评论/正文 → 运营复制到 X 手动发 → 面板「标记已发布/标记已评论」→ 自动写飞书记录表。自动发布通道（写入服务）暂停，等 X Developer Console credits 充值 + 9 个 OAuth2 重新授权（media.write）后启用。
- **运营手册**：`docs/x-console-manual/`（含截图 + Word docx + 构建脚本），给运营看。
- **依赖**：X OAuth 凭据、AdsPower、代理链（见 `infra/proxy/` + `infra/runbooks/proxy-chain.md`）、LLM（New API 8318，`WORKFLOW_LLM_KEY`）、飞书记录表。
- **已知坑**：
  - 代理链失效中（住宅代理 32 个凭据全 446 认证失败 + 闪电机场换域名），见 `infra/runbooks/proxy-chain.md`。
  - 写入账号 id=2 显示名标错（Pixel Mara 应是 Sato Reyes），纯展示 bug。
  - 评论配额护栏（5 条/号/天、≥15 分钟）待实现。

---

## 公共依赖与排障

- **定时任务全集**：`infra/systemd/`（18 个 unit，含 `.service` + `.timer`）。接手后逐个 `systemctl status` + `journalctl -u <名> --since "1 day ago"` 验活。
- **代理链**：`infra/proxy/chain-mapping.example.json`（端口→出口映射，脱敏）+ `infra/runbooks/proxy-chain.md`（排障手册）。代理是消耗品，凭据会过期，不在仓库。
- **飞书集成**：评论记录表 `P4Xcs`、发帖记录表 `PpGgg`、表格 `KezXs1UF8hXcdYtH8lechX8AnKf`、case 表 `FZjlwahqeic6OBkW9pRcbiWgnwd`。应用凭据在敏感清单。
- **凭据**：全在飞书【敏感】账号密码清单，仓库不含任何密钥。重建时照 `HANDOVER.md` 第 1–5 节填回 `.env`。
- **service-account.json 教训**：之前 Gmail 私钥误提交进仓，已删 + 抹历史 + gitignore。别再把含私钥的 json 提交（`.gitignore` 已加 `service-account*.json` / `credentials.json` / `gsc_token.json`）。
